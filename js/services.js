document.addEventListener("DOMContentLoaded", () => {
    const serviceMenu = document.getElementById("servicesMenu");
  
    if (serviceMenu) {
      console.log("✅ Services dropdown initialized");
      // Later: attach click handlers, modal triggers, etc.
    }
  });